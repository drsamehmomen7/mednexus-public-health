# Copy this file to ".env" (same folder) and fill in your real values.
# ".env" itself is gitignored — never commit real credentials.
#
# From: Render Dashboard -> mednexus-public-health-db -> Info page ->
# External Database URL (the copy icon next to it).

DATABASE_URL=postgresql://user:password@host.oregon-postgres.render.com/dbname
